#include <vector>

using namespace std;

void dfs(int lambs, int wolves, const vector<int>& info, const vector<vector<int> >& edges) {
    if (lambs == wolves) {
        lambs = 0;
    }
}

int solution(vector<int> info, vector<vector<int>> edges) {
    int answer = 0;
    return answer;
}

int main() {

    return 0;
}